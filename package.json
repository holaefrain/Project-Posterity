{
  "name": "vinyl-manager",
  "version": "1.0.0",
  "description": "AI-powered vinyl collection manager with DYMO label export and Discogs integration",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --import dotenv/config server.js",
    "dev": "node --watch --import dotenv/config server.js"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "node-fetch": "^3.3.0",
    "xlsx": "^0.18.5"
  }
}
